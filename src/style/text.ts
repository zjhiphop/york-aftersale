import { StyleSheet } from 'react-native';

const text = StyleSheet.create({
    p: {
        color: 'black',
        fontFamily: 'Open Sans',
        fontSize: 14
    },
    title: {

    }
});

export default text;