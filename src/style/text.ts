import { StyleSheet } from 'react-native';

const text = StyleSheet.create({
    p: {
        color: 'black',
        fontFamily: 'Open Sans',
        fontSize: 14
    },
    title: {
        fontSize: 14,
        color: 'blue',
        marginRight: 10
    }
});

export default text;